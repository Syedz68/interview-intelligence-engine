# ============================================================
# requirements_env_a.txt  –  service_audio  (.venv_audio)
# ============================================================
# numpy < 2  REQUIRED: pyannote.audio 3.3.2, numba 0.65.x,
# and tensorflow 2.21 all break on numpy >= 2.
#
# INSTALL ORDER (important – do NOT just pip install -r directly):
# ---------------------------------------------------------------
#   Step 1:  pip install "numpy==1.26.4"
#   Step 2:  pip install torch==2.2.2+cu121 torchaudio==2.2.2+cu121 \
#                --index-url https://download.pytorch.org/whl/cu121
#   Step 3:  pip install -r requirements_env_a.txt
#
# For CPU-only machines replace cu121 with cpu in Step 2.
# ============================================================

# Core numeric stack (numpy<2 pin)
numpy==1.26.4
scipy==1.17.1
numba==0.65.1
llvmlite==0.47.0

# PyTorch – install separately first (see above), then listed here for reference
# torch==2.2.2+cu121
# torchaudio==2.2.2+cu121
torchmetrics==1.9.0
einops==0.8.2

# Speech / Audio
openai-whisper==20250625
pyannote.audio==3.3.2
pyannote.core==5.0.0
pyannote.database==5.1.3
pyannote.metrics==3.2.1
pyannote.pipeline==3.0.1
speechbrain==1.1.0
asteroid-filterbanks==0.4.0
julius==0.2.7
pydub==0.25.1
soundfile==0.13.1
sounddevice==0.5.1
librosa>=0.10.0

# Hugging Face (PINNED – pyannote uses deprecated use_auth_token removed in >=0.24)
huggingface-hub==0.23.4
tiktoken==0.12.0

# FastAPI / Web
fastapi==0.136.0
uvicorn==0.44.0
starlette==1.0.0
pydantic==2.13.2
pydantic-settings==2.13.1
python-multipart==0.0.26
python-dotenv==1.2.2
requests==2.33.1

# Utilities
tqdm==4.67.3
ffmpeg-python==0.2.0
